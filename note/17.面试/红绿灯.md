``` html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>街头红绿灯系统</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #1e3c72 0%, #2a5298 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
            position: relative;
            overflow: hidden;
        }

        /* 背景城市剪影 */
        body::before {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            height: 200px;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 200"><polygon fill="%23000" opacity="0.3" points="0,200 0,100 100,80 150,120 200,60 250,90 300,50 350,100 400,40 450,80 500,30 550,70 600,20 650,90 700,45 750,85 800,35 850,75 900,25 950,95 1000,55 1050,85 1100,45 1150,75 1200,65 1200,200"/></svg>') no-repeat center bottom;
            background-size: cover;
        }

        .container {
            text-align: center;
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(15px);
            border-radius: 25px;
            padding: 30px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            border: 1px solid rgba(255, 255, 255, 0.1);
            max-width: 500px;
            margin: 0 auto;
        }

        h1 {
            color: white;
            font-size: 2.2rem;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
            margin-bottom: 30px;
        }

        /* 红绿灯主体 */
        .traffic-light-system {
            display: inline-flex;
            align-items: flex-end;
            gap: 15px;
            margin-bottom: 20px;
        }

        .traffic-light {
            background: linear-gradient(145deg, #2a2a2a, #1a1a1a);
            border-radius: 20px;
            padding: 20px 15px;
            width: 140px;
            box-shadow: 
                0 15px 35px rgba(0, 0, 0, 0.6),
                inset 0 2px 5px rgba(255, 255, 255, 0.1),
                inset 0 -2px 5px rgba(0, 0, 0, 0.3);
            border: 2px solid #333;
            position: relative;
        }

        .traffic-light::before {
            content: '';
            position: absolute;
            top: -10px;
            left: 50%;
            transform: translateX(-50%);
            width: 30px;
            height: 20px;
            background: linear-gradient(145deg, #333, #222);
            border-radius: 5px 5px 0 0;
            border: 1px solid #444;
        }

        .light-housing {
            background: rgba(0, 0, 0, 0.8);
            border-radius: 15px;
            padding: 8px;
            margin: 12px 0;
            border: 2px solid #444;
            box-shadow: inset 0 0 10px rgba(0, 0, 0, 0.8);
        }

        .light {
            width: 90px;
            height: 90px;
            border-radius: 50%;
            margin: 0 auto;
            border: 3px solid #222;
            transition: all 0.8s cubic-bezier(0.68, -0.55, 0.265, 1.55);
            position: relative;
            overflow: hidden;
            box-shadow: inset 0 0 20px rgba(0, 0, 0, 0.8);
        }

        .light::before {
            content: '';
            position: absolute;
            top: 15%;
            left: 25%;
            width: 35%;
            height: 35%;
            background: rgba(255, 255, 255, 0.4);
            border-radius: 50%;
            transform: rotate(-45deg);
            transition: opacity 0.3s ease;
        }

        .light.off::before {
            opacity: 0;
        }

        /* 灯光颜色和效果 */
        .red {
            background: radial-gradient(circle at 30% 30%, #666, #333);
        }

        .red.on {
            background: radial-gradient(circle at 30% 30%, #ff4444, #cc0000);
            box-shadow: 
                0 0 80px rgba(255, 0, 0, 0.8),
                inset 0 0 30px rgba(255, 255, 255, 0.1);
            border-color: #ff6666;
        }

        .yellow {
            background: radial-gradient(circle at 30% 30%, #666, #333);
        }

        .yellow.on {
            background: radial-gradient(circle at 30% 30%, #ffee44, #ffcc00);
            box-shadow: 
                0 0 80px rgba(255, 204, 0, 0.8),
                inset 0 0 30px rgba(255, 255, 255, 0.1);
            border-color: #ffee66;
        }

        .green {
            background: radial-gradient(circle at 30% 30%, #666, #333);
        }

        .green.on {
            background: radial-gradient(circle at 30% 30%, #44ff44, #00cc00);
            box-shadow: 
                0 0 80px rgba(0, 255, 0, 0.8),
                inset 0 0 30px rgba(255, 255, 255, 0.1);
            border-color: #66ff66;
        }

        /* 数字显示屏 */
        .countdown-display {
            background: linear-gradient(145deg, #1a1a1a, #0f0f0f);
            border: 2px solid #333;
            border-radius: 10px;
            width: 80px;
            height: 140px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            box-shadow: 
                0 10px 25px rgba(0, 0, 0, 0.6),
                inset 0 2px 5px rgba(255, 255, 255, 0.05);
            position: relative;
        }

        .countdown-display::before {
            content: '';
            position: absolute;
            inset: 6px;
            background: #000;
            border-radius: 6px;
            border: 1px solid #222;
        }

        .digit-display {
            background: #000;
            border: 1px solid #333;
            border-radius: 6px;
            width: 60px;
            height: 100px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Courier New', monospace;
            font-size: 2.8rem;
            font-weight: bold;
            color: #00ff00;
            text-shadow: 0 0 15px #00ff00;
            position: relative;
            z-index: 2;
            overflow: hidden;
        }

        .digit-display::before {
            content: '';
            position: absolute;
            inset: 0;
            background: 
                repeating-linear-gradient(
                    0deg,
                    transparent,
                    transparent 2px,
                    rgba(0, 255, 0, 0.1) 2px,
                    rgba(0, 255, 0, 0.1) 4px
                );
            pointer-events: none;
        }

        .digit-display.red-text {
            color: #ff0000;
            text-shadow: 0 0 20px #ff0000;
        }

        .digit-display.yellow-text {
            color: #ffff00;
            text-shadow: 0 0 20px #ffff00;
        }

        .digit-display.off {
            color: #333;
            text-shadow: none;
        }

        /* 控制按钮 */
        .controls {
            position: absolute;
            bottom: 30px;
            left: 50%;
            transform: translateX(-50%);
            display: flex;
            flex-wrap: wrap;
            gap: 15px;
            justify-content: center;
        }

        button {
            padding: 15px 30px;
            border: none;
            border-radius: 30px;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.3);
            text-transform: uppercase;
            letter-spacing: 1px;
            position: relative;
            overflow: hidden;
        }

        button::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s ease;
        }

        button:hover::before {
            left: 100%;
        }

        .btn-red {
            background: linear-gradient(45deg, #ff4444, #cc0000);
            color: white;
        }

        .btn-red:hover {
            background: linear-gradient(45deg, #ff6666, #ff0000);
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(255, 0, 0, 0.4);
        }

        .btn-yellow {
            background: linear-gradient(45deg, #ffdd44, #cc9900);
            color: #333;
        }

        .btn-yellow:hover {
            background: linear-gradient(45deg, #ffee66, #ffcc00);
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(255, 204, 0, 0.4);
        }

        .btn-green {
            background: linear-gradient(45deg, #44ff44, #00cc00);
            color: white;
        }

        .btn-green:hover {
            background: linear-gradient(45deg, #66ff66, #00ff00);
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(0, 255, 0, 0.4);
        }

        .btn-auto {
            background: linear-gradient(45deg, #667eea, #764ba2);
            color: white;
        }

        .btn-auto:hover {
            background: linear-gradient(45deg, #778beb, #8b5fbf);
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(102, 126, 234, 0.4);
        }

        .btn-stop {
            background: linear-gradient(45deg, #666, #333);
            color: white;
        }

        .btn-stop:hover {
            background: linear-gradient(45deg, #777, #444);
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.6);
        }

        /* 状态显示 */
        .status {
            position: absolute;
            top: 120px;
            left: 50%;
            transform: translateX(-50%);
            background: rgba(255, 255, 255, 0.1);
            border-radius: 20px;
            padding: 15px 30px;
            color: white;
            font-size: 18px;
            font-weight: bold;
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
        }

        /* 脉冲效果 */
        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.6; }
        }

        .light.on.pulse {
            animation: pulse 1s infinite;
        }

        @keyframes urgentPulse {
            0%, 100% { 
                transform: scale(1);
                text-shadow: 0 0 20px #ff0000;
            }
            50% { 
                transform: scale(1.1);
                text-shadow: 0 0 30px #ff0000, 0 0 40px #ff0000;
            }
        }

        .digit-display.urgent {
            animation: urgentPulse 0.8s infinite;
        }

        /* 响应式设计 */
        @media (max-width: 768px) {
            .container {
                flex-direction: column;
                gap: 20px;
                padding: 20px;
            }
            
            .traffic-light-system {
                flex-direction: row;
                gap: 15px;
            }
            
            .traffic-light {
                width: 120px;
                padding: 20px 15px;
            }
            
            .light {
                width: 80px;
                height: 80px;
            }
            
            .countdown-display {
                width: 80px;
                height: 120px;
            }
            
            .digit-display {
                width: 55px;
                height: 90px;
                font-size: 2.5rem;
            }
            
            h1 {
                font-size: 1.8rem;
            }
            
            .controls {
                position: relative;
                bottom: auto;
                left: auto;
                transform: none;
                margin-top: 20px;
            }
            
            button {
                padding: 12px 24px;
                font-size: 14px;
            }
        }
    </style>
</head>
<body>
    <h1>🚦 街头红绿灯系统</h1>
    
    <div class="container">
        <div class="traffic-light-system">
            <div class="traffic-light">
                <div class="light-housing">
                    <div class="light red off" id="redLight"></div>
                </div>
                <div class="light-housing">
                    <div class="light yellow off" id="yellowLight"></div>
                </div>
                <div class="light-housing">
                    <div class="light green off" id="greenLight"></div>
                </div>
            </div>
            
            <div class="countdown-display">
                <div class="digit-display off" id="countdownDigit">--</div>
            </div>
        </div>
    </div>
    
    <div class="status">
        <div id="statusText">点击按钮控制红绿灯</div>
    </div>
    
    <div class="controls">
        <button class="btn-red" onclick="setLight('red')">红灯</button>
        <button class="btn-yellow" onclick="setLight('yellow')">黄灯</button>
        <button class="btn-green" onclick="setLight('green')">绿灯</button>
        <button class="btn-auto" onclick="startAuto()">自动循环</button>
        <button class="btn-stop" onclick="stopAuto()">停止</button>
    </div>

    <script>
        class StreetTrafficLight {
            constructor() {
                this.redLight = document.getElementById('redLight');
                this.yellowLight = document.getElementById('yellowLight');
                this.greenLight = document.getElementById('greenLight');
                this.countdownDigit = document.getElementById('countdownDigit');
                this.statusText = document.getElementById('statusText');
                
                this.currentState = 'off';
                this.autoInterval = null;
                this.countdownInterval = null;
                this.timeLeft = 0;
                
                // 真实红绿灯时间配置
                this.timings = {
                    red: 30,    // 红灯30秒
                    green: 25,  // 绿灯25秒
                    yellow: 5   // 黄灯5秒
                };
                
                this.init();
            }
            
            init() {
                this.turnOffAllLights();
                this.updateStatus('系统就绪 - 等待指令');
                this.updateCountdown('--');
            }
            
            turnOffAllLights() {
                [this.redLight, this.yellowLight, this.greenLight].forEach(light => {
                    light.classList.remove('on', 'pulse');
                    light.classList.add('off');
                });
            }
            
            updateCountdown(value) {
                this.countdownDigit.textContent = value;
                
                // 根据当前灯光设置数字颜色
                this.countdownDigit.classList.remove('red-text', 'yellow-text', 'off');
                
                if (value === '--' || value === '0') {
                    this.countdownDigit.classList.add('off');
                } else if (this.currentState === 'red') {
                    this.countdownDigit.classList.add('red-text');
                } else if (this.currentState === 'yellow') {
                    this.countdownDigit.classList.add('yellow-text');
                } else {
                    // 绿灯保持默认绿色
                }
            }
            
            setLight(color) {
                this.stopAuto();
                this.turnOffAllLights();
                
                const lightMap = {
                    'red': this.redLight,
                    'yellow': this.yellowLight,
                    'green': this.greenLight
                };
                
                const statusMap = {
                    'red': '🔴 红灯 - 禁止通行',
                    'yellow': '🟡 黄灯 - 准备停车',
                    'green': '🟢 绿灯 - 安全通行'
                };
                
                if (lightMap[color]) {
                    lightMap[color].classList.remove('off');
                    lightMap[color].classList.add('on');
                    this.currentState = color;
                    this.updateStatus(statusMap[color]);
                    
                    // 手动模式显示固定倒计时
                    this.startCountdown(60);
                }
            }
            
            startAuto() {
                this.stopAuto();
                this.updateStatus('🔄 自动循环模式 - 模拟真实街头红绿灯');
                
                // 真实红绿灯序列：红 -> 绿 -> 黄 -> 红
                let sequence = [
                    { color: 'red', duration: this.timings.red },
                    { color: 'green', duration: this.timings.green },
                    { color: 'yellow', duration: this.timings.yellow }
                ];
                let currentIndex = 0;
                
                const nextLight = () => {
                    const current = sequence[currentIndex];
                    this.setLightWithoutStopAuto(current.color);
                    this.startCountdown(current.duration);
                    
                    this.autoInterval = setTimeout(() => {
                        currentIndex = (currentIndex + 1) % sequence.length;
                        nextLight();
                    }, current.duration * 1000);
                };
                
                nextLight();
            }
            
            setLightWithoutStopAuto(color) {
                this.turnOffAllLights();
                
                const lightMap = {
                    'red': this.redLight,
                    'yellow': this.yellowLight,
                    'green': this.greenLight
                };
                
                const statusMap = {
                    'red': '🔴 红灯 - 禁止通行',
                    'yellow': '🟡 黄灯 - 准备停车',
                    'green': '🟢 绿灯 - 安全通行'
                };
                
                if (lightMap[color]) {
                    lightMap[color].classList.remove('off');
                    lightMap[color].classList.add('on');
                    this.currentState = color;
                    this.updateStatus(statusMap[color] + ' (自动模式)');
                }
            }
            
            stopAuto() {
                if (this.autoInterval) {
                    clearTimeout(this.autoInterval);
                    this.autoInterval = null;
                }
                this.clearCountdown();
                this.updateStatus('⏹️ 自动模式已停止');
                this.updateCountdown('--');
            }
            
            startCountdown(seconds) {
                this.clearCountdown();
                this.timeLeft = seconds;
                
                const updateTimer = () => {
                    this.updateCountdown(this.timeLeft.toString().padStart(2, '0'));
                    
                    // 最后5秒添加紧急样式和脉冲效果
                    if (this.timeLeft <= 5 && this.timeLeft > 0) {
                        this.countdownDigit.classList.add('urgent');
                        const currentLight = document.querySelector('.light.on');
                        if (currentLight) {
                            currentLight.classList.add('pulse');
                        }
                    } else {
                        this.countdownDigit.classList.remove('urgent');
                        const currentLight = document.querySelector('.light.on');
                        if (currentLight) {
                            currentLight.classList.remove('pulse');
                        }
                    }
                    
                    if (this.timeLeft > 0) {
                        this.timeLeft--;
                        this.countdownInterval = setTimeout(updateTimer, 1000);
                    } else {
                        this.clearCountdown();
                    }
                };
                
                updateTimer();
            }
            
            clearCountdown() {
                if (this.countdownInterval) {
                    clearTimeout(this.countdownInterval);
                    this.countdownInterval = null;
                }
                
                this.countdownDigit.classList.remove('urgent');
                document.querySelectorAll('.light').forEach(light => {
                    light.classList.remove('pulse');
                });
            }
            
            updateStatus(text) {
                this.statusText.textContent = text;
            }
        }
        
        // 创建街头红绿灯实例
        const streetLight = new StreetTrafficLight();
        
        // 全局函数供按钮调用
        function setLight(color) {
            streetLight.setLight(color);
        }
        
        function startAuto() {
            streetLight.startAuto();
        }
        
        function stopAuto() {
            streetLight.stopAuto();
        }
        
        // 键盘快捷键
        document.addEventListener('keydown', function(event) {
            switch(event.key.toLowerCase()) {
                case 'r':
                    setLight('red');
                    break;
                case 'y':
                    setLight('yellow');
                    break;
                case 'g':
                    setLight('green');
                    break;
                case 'a':
                    startAuto();
                    break;
                case 's':
                    stopAuto();
                    break;
                case ' ':
                    event.preventDefault();
                    startAuto();
                    break;
            }
        });
        
        // 添加提示信息
        console.log('🚦 街头红绿灯快捷键:');
        console.log('R - 红灯 (30秒)');
        console.log('Y - 黄灯 (5秒)');  
        console.log('G - 绿灯 (25秒)');
        console.log('A - 自动循环');
        console.log('S - 停止');
        console.log('空格 - 开始自动循环');
    </script>
</body>
</html>
```

